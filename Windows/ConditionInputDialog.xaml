<Window x:Class="LangraphIDE.Windows.ConditionInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Route Condition"
        Width="350" Height="180"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource SurfaceBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Enter the route name for this connection:"
                   Foreground="{DynamicResource TextPrimaryBrush}"
                   FontSize="13"
                   Margin="0,0,0,8"/>

        <TextBlock Grid.Row="1"
                   Text="This should match the return value in your routing logic"
                   Foreground="{DynamicResource TextSecondaryBrush}"
                   FontSize="11"
                   Margin="0,0,0,12"/>

        <TextBox x:Name="ConditionTextBox"
                 Grid.Row="2"
                 VerticalAlignment="Top"
                 FontSize="14"
                 Padding="10,8"/>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Content="Cancel"
                    Width="80"
                    Padding="8,6"
                    Margin="0,0,8,0"
                    Click="Cancel_Click"
                    Style="{DynamicResource ToolbarButton}"/>
            <Button Content="OK"
                    Width="80"
                    Padding="8,6"
                    Click="OK_Click"
                    Style="{DynamicResource PrimaryButton}"/>
        </StackPanel>
    </Grid>
</Window>
